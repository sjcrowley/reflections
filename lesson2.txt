What happens when you initialize a repository? Why do you need to do it?

The .git folder is created which contains metadata for the repository.  It needs to be done to let the OS know that a directory should be treated as a git repository. 